// 最简类测试：只测试定义和实例化，不测试方法调用

class Point {
    var x: int
    var y: int

    ctor(a: int, b: int) {
        this.x = a
        this.y = b
    }
}

fun main(): void {
    var p = Point(10, 20)
    sout(p.x)
    sout(p.y)
}
