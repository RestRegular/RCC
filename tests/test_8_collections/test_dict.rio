fun sout(*args, end="\n"): void {
    encapsulated
}

// 测试字典创建
var dic = {"name": "Rio", "age": 18}

// 测试字典索引访问
sout(dic["name"])
sout(dic["age"])

// 测试更多键值对
var dic2 = {"a": 1, "b": 2, "c": 3}
sout(dic2["a"])
sout(dic2["b"])
sout(dic2["c"])
