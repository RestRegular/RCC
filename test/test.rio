import(io="io")

var sout: func[.., str] = io.sout

(fc: func[funi[int], int], fi1: funi[int], fi2: funi[int], a: int) -> {
    fc(fi1, fi2(a))
}(
    (fi: funi[int], a: int): void -> { sout(fi(a)) },
    (b: int) -> "Res: " + b;,
    (a: int) -> a * a;,
    10
)
